import Logo from '../Logo';

const Branding = () => {
    return( 
        <div id="logo">
            <a href="Home">
                <Logo />
            </a>
        </div>
    );
};

export default Branding;