import { app, FormContext, fieldStyles } from '@Form';

const Checks = ({
    column,
    items,
    checkedItems,
    show,
    choose
}) => {
    return <div className={fieldStyles}>

    </div>
}

export { Checks }